#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;  cin >> t;

    while (t--) {
        int alphabat, length = 0;
        int ans;
        cin >> length >> alphabat;
        
        for (int i = 0; i <= alphabat; i++)
        {
            
        }
        

        for (int i = 1; i <= length; i++) {
            /* code */
        }


        printf("%c", 97);


    }


}