#include <iostream>
#include <string>
using namespace std;


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;

    while (t--) {
        int len, num_of_b;
        int op = 0;
        cin >> len >> num_of_b;
        string str; cin >> str;
        while (len--) {
        }

        cout << op << "\n";
        cout << index << "\n";

    }


}